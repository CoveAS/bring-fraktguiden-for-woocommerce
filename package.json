{
  "name": "bring-fraktguiden-for-woocommerce",
  "description": "Bring Fraktguiden provides shipping calculations based on shipping rates from bring.no",
  "main": "vite.config.js",
  "scripts": {
    "clean": "rm -rf build",
    "compile-php": "php bin/compile-templates.php",
    "test-php-compiler": "php bin/test-compiler.php",
    "test-js": "node --test 'tests/js/*.test.mjs'",
    "dev": "npm run clean && vite build --mode development && npm run compile-php",
    "build": "npm run clean && vite build && npm run compile-php",
    "watch": "npm run clean && vite build --watch",
    "production": "npm run clean && vite build --mode production && npm run compile-php"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.1",
    "@tailwindcss/vite": "^4.0.0",
    "@vitejs/plugin-vue": "^5.2.4",
    "postcss": "^8.5.28",
    "postcss-import": "^16.1.1",
    "tailwindcss": "^4.0.0",
    "vite": "^6.4.3",
    "vue": "^3.2.31"
  },
  "dependencies": {
    "canvas-confetti": "^1.9.4"
  }
}
